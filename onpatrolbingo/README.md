# On Patrol Live BINGO → moved

This game is now **Game Night BINGO** at [`../bingo/`](../bingo/) — the same engine
(rooms, teams, shared cards, patterns, packs) with a theme picker, so On Patrol
and football nights live in one app.

`index.html` here is a redirect, kept so home-screen shortcuts and old links still work.
