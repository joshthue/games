# On Patrol Live BINGO

A single-file bingo game you play along with the show. Tap squares as they happen
and chase a line (or a full card).

## Running it

Open `index.html` in a browser — no build step or dependencies. Works offline once
loaded, as part of the Game Night home-screen app.
